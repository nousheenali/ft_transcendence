version: '3.9'
services:
  db:
    build:
      context: .
      dockerfile: postgres_db/Dockerfile
    ports:
      - '${DB_PORT}:5432'
    volumes:
      - db-data:/var/lib/postgresql/data
    env_file:
      - .env

volumes:
  db-data:
